// import React from 'react';
// import Form from 'react-json-editor';
// import ReactDOM from 'react-dom';

// const schema = {
//   title: 'My pretty form',
//   description: 'Declarative pure data DSLs are the best.',
//   type: 'object',
//   properties: {
//     comment: {
//       title: 'Your comment',
//       description: 'Type something here.',
//       type: 'string',
//       minLength: 1
//     }
//   }
// };

// const onSubmit = function(data, buttonValue, errors) {
//   alert(
//     `Data  : ${JSON.stringify(data)}\n` +
//       `Button: ${buttonValue}\n` +
//       `Errors: ${JSON.stringify(errors)}`
//   );
// };

// ReactDOM.render(<Form schema={schema} onSubmit={onSubmit} />, document.body);
